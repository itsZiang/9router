// Stub: usage history — no-op tracking.

export function finalizeMostRecentPendingRequest(model, provider, connectionId, metadata) {
  return false;
}

export function finalizePendingRequestById(id, metadata) {
  return false;
}