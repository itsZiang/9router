// Stub: token accounting — returns zero for all token counts.

export function getLoggedInputTokens(tokens) {
  return 0;
}

export function getLoggedOutputTokens(tokens) {
  return 0;
}

export function getReasoningTokens(tokens) {
  return 0;
}